#include <stdio.h>
#include <stdlib.h>
#include "input.h"

int main() {
    int n = lese_int();
    int resultat = 1;
    for (int i=n; i>0; i--) {
        resultat = resultat * i;
    }
    printf("%d\n", resultat);
    return 0;
}
